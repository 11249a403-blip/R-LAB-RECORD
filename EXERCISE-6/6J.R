# 10. Real-World Summary Question
# Scenario:
# Demonstrate use of vector, list, and data frame.

# Question:
# Explain with example.

scores <- c(85, 90, 88)

profile <- list(
  Name = "Bhavya",
  Age = 20,
  Scores = scores
)

records <- data.frame(
  Subject = c("Math", "CS", "Physics"),
  Marks = scores
)

scores
profile
records
