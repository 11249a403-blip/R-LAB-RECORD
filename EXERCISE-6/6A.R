# 1. Student Marks Processing (Vectors)
# Scenario:
# A lecturer stores marks of students and calculates total, average, and highest.

# Question:
# Use a suitable data structure and perform calculations.

marks <- c(88, 92, 79, 85, 90)

total <- sum(marks)
average <- mean(marks)
highest <- max(marks)

total
average
highest
