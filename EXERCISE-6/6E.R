# 5. College Student Database (Data Frames)
# Scenario:
# Store student details in tabular form.

# Question:
# Use data frame to store structured data.

students <- data.frame(
  RollNo = c(1, 2, 3),
  Name = c("Bhavya", "Karthik", "Meena"),
  Dept = c("IT", "ECE", "CSE"),
  Marks = c(85, 78, 92)
)

students
